[project]
name = "book-backend"
version = "0.1.0"
description = "FastAPI backend for a local-SQL book management system."
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic",
]

[tool.uvicorn]
factory = false
